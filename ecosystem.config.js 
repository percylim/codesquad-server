module.exports = {
  apps: [
    {
      name: "backend",
      script: "./server.js",
      instances: 1,           // change to "max" to use all CPU cores (cluster mode)
      exec_mode: "fork",      // or "cluster"
      watch: false,           // true for dev, false for prod
      ignore_watch: ["node_modules", "logs", ".git", "uploads", "tmp"],
      env: {
        NODE_ENV: "development",
        PORT: 3000
        // DB_HOST: "...", DB_USER: "...", etc. (or use dotenv in server.js)
      },
      env_production: {
        NODE_ENV: "production",
        PORT: 3000
      },
      max_memory_restart: "300M",
      merge_logs: true,
      log_date_format: "YYYY-MM-DD HH:mm:ss"
    }
  ]
};